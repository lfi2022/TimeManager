{
  "name": "@lfinfo/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lfinfo/shared": "workspace:*",
    "@fastify/static": "^8.3.0",
    "dotenv": "^17.2.0",
    "fastify": "^5.12.5",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
