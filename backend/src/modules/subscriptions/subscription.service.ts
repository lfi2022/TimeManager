import type { PrismaClient } from '@prisma/client';
import { z } from 'zod';
import { withCompanyId } from '../../tenancy/tenant-prisma.js';
import { AuditService } from '../audit/audit.service.js';
const assignment=z.object({planId:z.string().uuid()}).strict();
export class SubscriptionService { private readonly audit:AuditService; constructor(private readonly prisma:PrismaClient){this.audit=new AuditService(prisma);} async plans(){return this.prisma.plan.findMany({where:{active:true},orderBy:{name:'asc'}});} async summary(companyId:string){return withCompanyId(this.prisma,companyId,async tx=>{const [subscription,activeUsers]=await Promise.all([tx.subscription.findUnique({where:{companyId},include:{plan:true}}),tx.user.count({where:{companyId,active:true}})]);return{subscription,activeUsers};});} async assign(platformUserId:string,companyId:string,raw:unknown){const d=assignment.parse(raw);const plan=await this.prisma.plan.findFirst({where:{id:d.planId,active:true}});if(!plan)return null;const subscription=await withCompanyId(this.prisma,companyId,tx=>tx.subscription.upsert({where:{companyId},update:{planId:plan.id,status:'ACTIVE',suspendedAt:null},create:{companyId,planId:plan.id}}));await this.audit.recordPlatform(companyId,{action:'SUBSCRIPTION_ASSIGNED',entityType:'Subscription',entityId:subscription.id,platformUserId,metadata:{plan:plan.code}});return subscription;} async suspend(platformUserId:string,companyId:string,suspended:boolean){const result=await withCompanyId(this.prisma,companyId,async tx=>{const x=await tx.subscription.findUnique({where:{companyId}});return x?tx.subscription.update({where:{companyId},data:{status:suspended?'SUSPENDED':'ACTIVE',suspendedAt:suspended?new Date():null}}):null;});if(result)await this.audit.recordPlatform(companyId,{action:suspended?'SUBSCRIPTION_SUSPENDED':'SUBSCRIPTION_REACTIVATED',entityType:'Subscription',entityId:result.id,platformUserId});return result;} async supportContext(platformUserId:string,companyId:string){const summary=await this.summary(companyId);await this.audit.recordPlatform(companyId,{action:'SUPPORT_CONTEXT_SELECTED',entityType:'Company',entityId:companyId,platformUserId});return summary;}}