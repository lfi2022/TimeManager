import { useState } from 'react';
import { useNavigate } from 'react-router';
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { api, fetchHealth } from '../lib/api';
export function HomePage(){const nav=useNavigate(),client=useQueryClient();const health=useQuery({queryKey:['health'],queryFn:fetchHealth,refetchInterval:30000,retry:1});const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const login=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await api('/api/auth/login',{method:'POST',body:JSON.stringify({email,password})});await client.invalidateQueries({queryKey:['me']});nav('/dashboard')}catch(err){setError(err instanceof Error ? err.message : 'Connexion impossible. Vérifiez vos identifiants puis réessayez.')}finally{setBusy(false)}};return <div className="tp-landing"><header className="tp-header"><a href="/" aria-label="TempoPoint, accueil"><img src="/brand/logo-color.svg" alt="TempoPoint"/></a><div className={'tp-status '+(health.isSuccess?'is-up':'is-down')}><span/>{health.isPending?'Vérification du service…':health.isSuccess?'Tous les systèmes sont opérationnels':'Service temporairement indisponible'}</div></header><main><section className="tp-hero"><div className="tp-copy"><p className="tp-kicker">TEMPOPOINT <b>—</b> BY LFINFO</p><h1>Le temps de vos équipes,<br/><strong>simplement.</strong></h1><p>Pointage mobile, encodage des prestations et validation administrative dans une seule plateforme pensée pour les équipes de terrain.</p><div className="tp-proof"><span><b>01</b> Saisissez sur le terrain</span><span><b>02</b> Validez au bureau</span><span><b>03</b> Exportez sereinement</span></div></div><div className="tp-visual"><div className="tp-orb tp-orb-one"/><div className="tp-orb tp-orb-two"/><img src="/brand/og-image.png" alt="Interface TempoPoint"/><div className="tp-float-card"><img src="/brand/symbol.svg" alt=""/><span><b>Temps maîtrisé</b><small>Une vue claire, chaque jour.</small></span></div></div></section><section className="tp-access"><div className="tp-access-intro"><p className="tp-kicker">VOTRE ESPACE</p><h2>Prêt à retrouver<br/>votre équipe ?</h2><p>Connectez-vous à votre espace sécurisé TempoPoint.</p></div><form onSubmit={login}><label>Adresse e-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="prenom@entreprise.be" required/></label><label>Mot de passe<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="form-error">{error}</p>}<button className="tp-primary" disabled={busy}>{busy?'Connexion en cours…':'Accéder à mon espace'} <span>→</span></button><p className="tp-form-note">Un problème de connexion ? Contactez votre administrateur.</p></form></section></main><footer><img src="/brand/logo-white.svg" alt="TempoPoint"/><span>© {new Date().getFullYear()} LFINFO · Gestion du temps professionnel</span><a href="/platform">Portail LFINFO</a><a href="/api/health">État du service</a></footer></div>}